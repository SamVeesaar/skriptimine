* root kasutajana:
install - apt-get install git
* tavakasutajana:
esmakordselt:

a) seadista - m채채ra kasutajanimi, email, editor
git config --global user.name "S홢m Veesaar"
git config --global user.email sam.veesaar@khk.ee
git config --global core.editor nano

b) loo kataloog, kus hakkad skriptima
mkdir skriptimine

c) sisene antud kausta
cd skriptimine

d) tekita lokaalne repositoorium git s체steemi jaoks antud kaustas
git init

remote seadistamine:

a) git remote add nimi aadress
git remote add skriptimine https://github.com/kasutajanimi/reponimi.git

b) git remote kontroll
git remote -v

p천hikasutamine:

a) pane fail j채lgimisele:
git add failinimi

b) lisa kirjelduse muudatuse kohta
git commit -m "l체hikirjelduse lause"

c) lisa kaugrepositooriumi
git push -uf skriptimine master
